import java.util.*;

public class lec_11 {
    public static void main(String[] args) {

        // 2D Arrays

        //------------------------------------------------------------------------------------------------------------

        // Scanner sc = new Scanner(System.in);

        // // No of rows
        // System.out.print("Number of rows : ");
        // int rows = sc.nextInt();

        // // No of rows
        // System.out.print("Number of columns : ");
        // int columns = sc.nextInt();

        // // Create a 2D Array
        // int numbers[][] = new int[rows][columns];

        // // Take input for array elements
        // // outer loop
        // for (int i = 0; i < rows; i++) {
        //     for (int j = 0; j < columns; j++) {
        //         System.out.print("Enter element to put in array at " + "[" + i + "]" + "[" + j + "] : ");
        //         numbers[i][j] = sc.nextInt();
        //     }
        // }

        // // Print outputs for array elements
        // System.out.println("\n2D Array Matrix : ");
        // // outer loop
        // for (int i = 0; i < rows; i++) {
        //     // inner loop
        //     for (int j = 0; j < columns; j++) {
        //         System.out.print(numbers[i][j] + " ");
        //     }
        //     System.out.println();
        // }

        //------------------------------------------------------------------------------------------------------------------

        // Question - 1 : 

        // Scanner sc = new Scanner(System.in);

        // //Take input for rows and columns
        // System.out.print("Number of Rows : ");
        // int rows = sc.nextInt();
        // System.out.print("Number of Columns : ");
        // int columns = sc.nextInt();

        // //Create a 2D Array 
        // int numbers [][] = new int[rows][columns];

        // //Take array elements as input
        // for(int i=0 ; i<rows; i++){
        //     for(int j=0 ; j<columns ; j++){
        //         System.out.print("Enter value at " + "[" + i + "]" + "[" + j + "]" + " : ");
        //         numbers[i][j] = sc.nextInt();
        //     }
        // }

        // System.out.println("\n 2D Array matrix : ");
        // //Print array elements as output
        // for(int i=0 ; i<rows ; i++){
        //     for(int j=0 ; j<columns ; j++){
        //         System.out.print(numbers[i][j] + " ");
        //     }
        //     System.out.println();
        // }

        // // Searching for an element
        // System.out.print("Enter the number to be searched : ");
        // int x = sc.nextInt();

        // for(int i=0 ; i<rows ; i++){
        //     for(int j=0 ; j<columns ; j++){
        //         if(numbers[i][j] == x){
        //             System.out.println("Number " + numbers[i][j] + " found at indice " + "[" + i + "]" + "[" + j + "]" + " : ");
        //         }
        //     }
        // }

        //----------------------------------------------------------------------------------------------------------------------------------------------------------------

        

    }
}